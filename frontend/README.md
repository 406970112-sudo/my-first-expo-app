# 前端说明

这里是 Expo 前端项目。

常用启动方式：

```bash
cd frontend
npm install
npm run web
```

如果你要改页面，优先看这些目录：

- `app/`
- `components/`
- `constants/`
- `hooks/`

前端环境变量示例在：

[frontend/.env.example](c:/Users/Administrator/Desktop/my-first-expo-app/frontend/.env.example)
